"""Tool handlers — thin wrappers over existing platform services.

Each module gathers real platform data (assets, CVEs, tickets, news, RAG,
learning, reports, system status) so the analyst answers from the platform's
own knowledge before ever falling back to the LLM.
"""
