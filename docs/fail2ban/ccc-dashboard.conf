# fail2ban filter for the Cyber Command Center dashboard.
# Matches 401/429 responses in a reverse-proxy access log (combined format).
# Place at /etc/fail2ban/filter.d/ccc-dashboard.conf
[Definition]
failregex = ^<HOST> .* "(GET|POST) /(api|)[^"]*" (401|429)
ignoreregex =
